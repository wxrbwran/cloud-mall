
---
spring:
  profiles: prod
  application:
    name: cloud-mall-user
  cloud:
    config:
      name: cloud-mall-user
      uri: http://localhost:8301
      label: master
      profile: prod

---
spring:
  profiles: dev
  application:
    name: cloud-mall-user
  cloud:
    config:
      name: cloud-mall-user
      uri: http://localhost:8301
      label: master
      profile: dev